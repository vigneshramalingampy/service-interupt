from fastapi import UploadFile


class InterruptDatabase:

    @staticmethod
    async def upload_file(unique_id: str, file: UploadFile) -> None:
        try:
            pass
        except Exception as exception:
            pass
